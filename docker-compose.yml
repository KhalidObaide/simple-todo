services:
  backend:
    build: ./backend
    volumes:
      - todo-data:/data
    expose:
      - "6001"

  frontend:
    build: ./frontend
    ports:
      - "8080:8080"
    depends_on:
      - backend

volumes:
  todo-data:
